<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <title><%= title || "Error" %></title>
  <style>
    body {
      background-color: black;
      font-family: Arial, sans-serif;
      color: black;
      text-align: center;
      padding: 20px;
    }
    .message-container {
      margin: 50px auto;
      padding: 20px;
      border: 2px solid #ccc;
      border-radius: 10px;
      background-color: gainsboro;
      max-width: 500px;
    }
    .success-message {
      color: green;
      font-size: 1.2em;
      margin-bottom: 20px;
    }
    .error-message {
      color: red;
      font-size: 1.2em;
      margin-bottom: 20px;
    }
    .donation-details {
      text-align: left;
      margin-top: 20px;
    }
    .donation-details p {
      margin: 5px 0;
    }
    .links {
      margin-top: 20px;
    }
    .links a {
      font-size: 1.2em;
      margin: 10px;
      color: blue;
      text-decoration: none;
    }
    .links a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="message-container">
    <% if (typeof message !== "undefined") { %>
      <% if (typeof data !== "undefined" && data) { %>
        <h1 class="success-message"><%= message %></h1>
        <div class="donation-details">
          <h3>Donation Details:</h3>
          <p><strong>Food Type:</strong> <%= data.foodType || "N/A" %></p>
          <p><strong>Quantity:</strong> <%= data.quantity || "N/A" %></p>
          <p><strong>Pickup Date:</strong> <%= data.pickupDate || "N/A" %></p>
          <p><strong>Location:</strong> <%= data.completeAddress || "N/A" %></p>
          <p><strong>Contact Info:</strong> <%= data.contactInfo || "N/A" %></p>
        </div>
      <% } else { %>
        <h1 class="error-message"><%= message %></h1>
      <% } %>
    <% } else { %>
      <h1 class="error-message">Something went wrong! Please try again later.</h1>
    <% } %>

    <div class="links">
      <a href="/donate">Go back to the donation form</a>
      <% if (typeof links !== "undefined" && links) { %>
        <a href="<%= links.findDonation || '/' %>">Find Donations</a>
      <% } %>
    </div>
  </div>
</body>
</html>
