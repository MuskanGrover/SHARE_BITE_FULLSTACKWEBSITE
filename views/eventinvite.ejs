<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= eventName %></title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #1a1a2e;
      color: #f0f0f5;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: flex-start;
      min-height: 100vh;
      margin: 0;
      padding: 20px;
    }
    .container {
      background: #16213e;
      border-radius: 10px;
      padding: 30px;
      max-width: 700px;
      box-shadow: 0 0 15px rgba(0,0,0,0.7);
      text-align: center;
    }
    h1 {
      color: #e94560;
      margin-bottom: 5px;
    }
    p {
      font-size: 1.1rem;
      margin: 10px 0;
    }
    .event-info {
      margin: 20px 0;
      font-weight: bold;
    }
    iframe {
      width: 100%;
      height: 300px;
      border: none;
      border-radius: 10px;
      margin-bottom: 20px;
    }
    .btn {
      background: #e94560;
      color: white;
      padding: 15px 30px;
      border: none;
      border-radius: 8px;
      font-size: 1.2rem;
      cursor: pointer;
      text-decoration: none;
      display: inline-block;
      margin-top: 10px;
      transition: background 0.3s ease;
    }
    .btn:hover {
      background: #d73756;
    }
    footer {
      margin-top: 40px;
      font-size: 0.9rem;
      color: #aaa;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1><%= eventName %></h1>
    <p><%= description %></p>

    <div class="event-info">
      <p>📍 <strong>Location:</strong> <%= locationName %></p>
      <p>📅 <strong>Date:</strong> <%= date %></p>
      <p>⏰ <strong>Time:</strong> <%= time %></p>
    </div>

    <iframe src="<%= mapEmbed %>" allowfullscreen="" loading="lazy"></iframe>

    <a href="/mealreward" class="btn">RSVP & Claim Your Free Meal Coupon</a>
  </div>

  <footer>
    &copy; 2025 Meal of Happiness. Thank you for your generosity!
  </footer>
</body>
</html>
